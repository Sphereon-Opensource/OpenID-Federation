{
  "configurations": [
    {
      "name": "OpenID-Federation [:openid-federation-admin-server:build]",
      "type": "gradle",
      "workingDir": "$PROJECT_DIR$",
      "tasks": [
        ":modules:openid-federation-admin-server:build"
      ],
      "args": [
        ""
      ],
      "initScripts": {
        "flmapper": "ext.mapPath = { path -> null }"
      }
    },
    {
      "name": "OpenID-Federation [build]",
      "type": "gradle",
      "workingDir": "$PROJECT_DIR$",
      "tasks": [
        "build"
      ],
      "args": [
        ""
      ],
      "initScripts": {
        "flmapper": "ext.mapPath = { path -> null }"
      }
    }
  ]
}
