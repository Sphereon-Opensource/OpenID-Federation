package com.sphereon.oid.fed.client.services.entityConfigurationStatementService

import com.sphereon.oid.fed.logger.Logger

object EntityConfigurationStatemenServiceConst {
    private const val LOG_NAMESPACE = "sphereon:oidf:client:entityConfigurationStatementService"
    val LOG = Logger.tag(LOG_NAMESPACE)
}
