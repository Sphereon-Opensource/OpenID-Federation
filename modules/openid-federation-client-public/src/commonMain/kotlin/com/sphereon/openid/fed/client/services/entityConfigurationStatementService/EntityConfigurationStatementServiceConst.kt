package com.sphereon.openid.fed.client.services.entityConfigurationStatementService

import com.sphereon.core.api.log.Log

object EntityConfigurationStatementServiceConst {
    private const val LOG_NAMESPACE = "sphereon:oidf:client:entityConfigurationStatementService"
    val LOG = Log.app().withTag(LOG_NAMESPACE)
}
