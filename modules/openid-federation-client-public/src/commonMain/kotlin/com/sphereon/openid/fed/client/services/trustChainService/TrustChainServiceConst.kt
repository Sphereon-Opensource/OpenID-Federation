package com.sphereon.openid.fed.client.services.trustChainService

import com.sphereon.core.api.log.Log

object TrustChainServiceConst {
    private const val LOG_NAMESPACE = "sphereon:oidf:client:trustChainService"
    val LOG = Log.app().withTag(LOG_NAMESPACE)
}
