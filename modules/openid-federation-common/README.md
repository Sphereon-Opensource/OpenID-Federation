# Open Federation common

<br>

Usage of Logger
```
Logger.verbose("TAG", "Verbose log")
Logger.debug("TAG", "Debug log")
Logger.info("TAG", "Info log")
Logger.warn("TAG", "Warn log")
Logger.error("TAG", "Error log", exception)
```